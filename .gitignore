# Secrets — never commit
.env
*.secret

# Large source material (re-downloadable from archive.org)
source/jp2/
source/jp2.zip
source/honeywood.epub
source/honeywood.txt
source/epub_extracted/
source/honeywood_full.txt

# Intermediate OCR / QA outputs (regeneratable; not needed at runtime)
source/ocr_text/
source/letters_index.json
source/date_anomalies.json
source/date_outliers.json

# Generated previews / screenshots
*.png
# but DO track the site/email image assets:
!assets/*.png
.playwright-mcp/

# Generated static site (built by the workflow, deployed to Pages)
site/

# Cloudflare Worker (wrangler) local state
worker/.wrangler/
node_modules/

# Python
__pycache__/
*.pyc

# macOS
.DS_Store

# Reference EPUB (copyrighted US edition) + derived text/images — never commit
reference/
